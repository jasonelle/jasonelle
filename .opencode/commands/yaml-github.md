---
description: Lint and format YAML files inside .github/.
---

Lint and format the YAML files in this project, specifically inside `.github/`, using the current project `@.opencode/command/yaml.md` command.
