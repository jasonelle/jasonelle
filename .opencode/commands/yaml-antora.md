---
description: Lint and format YAML file antora-playbook.yml and antora/antora.yml.
---

Lint and format the YAML files `antora-playbook.yml` and `antora/antora.yml`, using the current project `@.opencode/command/yaml.md` command.
