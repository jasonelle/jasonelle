# lib/common/sources

Contain native code plugins for `android` and `xcode`.
Plugins that are just for `android` will not have `xcode` directory.
Plugins that are just for `xcode` will not have `android` directory.
Plugins that are for both platforms will have `android` and `xcode` directory.

Check `JLPluginHello` as an example plugin.
